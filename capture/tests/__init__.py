"""Tests for the capture service.

A package, not a bare directory: `ml/tests/` already has a `test_config.py`,
and without a package here pytest would resolve both to the same module name
and refuse to collect them.
"""
